# Local development configuration
# Override settings here for local development

muserver {
  port = 8080
  host = "0.0.0.0"
}

dag-agent {
  agentUrl = "http://localhost:8080"
  dagServerUrl = "http://localhost:8081"
  agentId = "agent-local-1"
  autoRegister = true
  maxConcurrentTasks = 4
  maxPendingTasks = 100
}
